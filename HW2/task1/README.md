# ICP

### Quick run
```bash
$ cd <Homework-2>/task1

# make sure to install Python dependencies with pip install -r requirements.txt

# Run the task of ICP with known correspondence
$ python task1.py -t 1 -d <folder-of-student-data>/student_data_45

# Run the task of ICP without known correspondence
$ python task1.py -t 2 -d <folder-of-student-data>/student_data_45
```
